{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Python in Terminal",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "preLaunchTask": "Run Python in Terminal"
        }
    ]
}
